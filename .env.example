# GA4 Web data stream measurement ID. Analytics are disabled when this is unset.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Optional: emit events to GA4 DebugView in a production build.
# VITE_GA_DEBUG=true
